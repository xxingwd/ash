# Ash 协作与 Workflow 重构 TODO

> 状态：核心实现已落地，进入补充验收。已移除旧协作 / JS workflow 入口，不提供兼容层。勾选表示该项已有代码和对应自动化验证；真实模型质量、终端手工验收及尚未覆盖的故障矩阵保留未勾选。
> 第一部分是行为的唯一权威定义；实施任务和测试通过 C 编号引用，不再维护另一份语义。
> `COLLABORATION.md` 与 `DESIGN.md` 已同步当前实现；本文记录唯一行为契约及尚待验收的具体差距。

## 一、已确认契约

### C01：对象与首版范围

- Agent 是一个独立、可持续交互的会话；普通委派不要求 group。
- Group 是共享上下文的串行工作线，同时最多一个直接成员运行；不同 group 与独立 agent 可以并行。
- Workflow 管理者是选择 workflow profile 的普通子 agent，复用 Agent / Session；workflow skill 提供使用方法，workflow 工具校验和装配组织，不新增 Agent 执行类型或流程引擎。
- Runtime 指确定性执行侧：保证身份、工具、调度、持久化与真实副作用，不因此新增万能 runtime crate。
- 完成只表示目标停止运行，不代表业务成功；parent 接收结果后决定继续、改派或结束。
- 首版不实现 worktree、分支隔离、自动提交 / 合并、TODO 自动推进、完成投票或自动总结。
- 没有代码隔离时，可能写同一文件的任务由协调 agent 避免并行安排；保留 bash 的角色不宣称是只读沙箱。
- 不增加模型可见的 stop、remove、handoff、finish、run_group、ack 或历史轮次查询工具；runtime / UI 仍可取消和关闭。

### C02：Profile 与内置角色

基础内置定义为 `crates/ash-agent/agents/default.md`、`explore.md`、`review.md`；另由 ash-workflow 提供同格式的 `agents/workflow.md` 并通过装配入口注册为 workflow profile，避免 ash-agent 反向依赖 workflow。文件名是不含扩展名的 profile 标识，不再声明重复的 name / id；不创建角色子目录或集中式 profiles.yaml。

```markdown
---
description: 独立审查代码正确性与回归风险
tools: "read, glob, grep, bash, webfetch"
---

结合上下文核实正确性问题与回归风险，说明触发条件、影响和代码位置。
没有发现问题时如实说明检查范围，不编造审查意见。
```

| 部分 | 定义 |
| --- | --- |
| description | 必填的非空用途说明，供调用者选择角色 |
| tools | 可选的逗号分隔字符串；去除名称首尾空白、去重；未知工具 / 空条目报错，整个空串表示不选择常规工具 |
| 正文 | 必填的非空角色 System 指令，只写职责、工作原则与交付要点，不重复基础规则和协作说明 |
| 省略 tools | 使用当前默认全部常规工具，不表示继承 parent 的组织管理能力 |
| 其他字段 | 首版不支持 model、workflow 配置字段、继承、权限表达式或通配符；不支持的字段报错，角色名 workflow 不等于增加同名配置字段 |

| 角色 | 职责 | 初始常规工具 |
| --- | --- | --- |
| default | 通用分析、实施与验证，按任务需要推进 | 省略 tools，与当前默认工具集一致 |
| explore | 定位实现、梳理关系与影响范围，区分事实和假设 | 默认工具集去掉 write、edit；当前为 read、glob、grep、bash、webfetch |
| review | 独立核实问题、触发条件和回归影响，不凑审查意见 | 与 explore 相同 |
| workflow | 根据任务制定蓝图、创建组织、跨组分配工作、接收结果并处理异常 | 默认常规工具；创建管理会话时另行安装 C11 的组织装配和协作能力 |

- profile 参数省略时使用 default；显式空值、空名称或未知名称报错。根会话与 workflow 节点也使用同一默认规则。
- 创建实例时固定解析后的 profile 标识、正文和初始常规工具选择；后续 message 不重选角色，恢复不以新版同名角色替换旧实例。
- 内置文件编译进二进制，首版无用户 / 项目覆盖、热加载或运行中角色切换。新版本的新实例可使用新定义，旧实例仍恢复自己的定义。
- 同一 profile 创建的实例不共享个人历史；角色既可用于普通会话，也可用于组员或协调 agent。

### C03：提示词组成与 AGENTS.md

System 只组合三类来源：**显式定义的提示词 + 已安装工具注入的说明 + 各级适用的 AGENTS.md**。显式部分包括基础行为、选定角色，以及装配时需要的 workflow 协调指令。

| 来源 | 维护方 | 装配边界 |
| --- | --- | --- |
| 基础行为、环境、仓库指令 | ash-agent 的对应模块 | 使用实际 cwd；环境沿用既有上下文路径 |
| 角色正文 | profile 文件 | 按 C02 固定，不复制所有角色全文 |
| 工具说明和依赖 | 各工具 / 能力所属模块 | 安装时一起提供，未安装的工具不残留行为说明 |
| 可选角色名称与用途 | agent 创建工具 | 只列实际可创建项，按名称排序；不另加 list_profiles |
| Skill 内容与工具说明 | skills 及其工具 | 按 C04 激活，不在 System 与工具输出重复灌入同一正文 |
| Workflow 管理者职责 | ash-workflow 的 agents/workflow.md | 创建 workflow profile 子会话时安装；普通主会话和内部成员不自动继承 |
| Workflow 用法与蓝图示例 | ash-workflow 的 skills/workflow/SKILL.md | 管理者按需读取；不重复角色职责，读取不改变已预装工具集合 |
| Group prompt、当前消息和关系 | group 文件、collab 与调用者 | 属于当前执行上下文，保留来源，不升级成角色权限 |

- 组合器只接收具名、有序内容，不包含 review / workflow 等业务分支；CLI 只选择安装，不编写各模块正文。
- AGENTS.md 初始按项目根到 cwd 的目录链加载；操作更深目录时发现并补充目标作用域的指令，不全仓扫描、不把兄弟目录规则全局化。
- 文件写入前必须让模型看到新发现的适用规则；尚未提供时先返回规则，本次不执行写入，由后续调用继续。相同请求批次也不能绕过此检查。
- bash 的任意脚本不能可靠推导全部目标路径；工具说明要求操作新目录前检查适用规则，不宣称实现 shell 文件访问沙箱。
- 文件规则保留路径、作用域和既有覆盖关系；超长内容明确截断，读取错误不得伪装成没有规则。
- Group prompt 每个成员 turn 开始时取快照；当前消息不进入静态角色正文。重试、压缩和多次模型请求不能重复累积动态片段。
- Runtime 身份和组织限制由工具入口落实，不靠提示词禁止创建下级；可编辑共享文件不能解除这些限制。

参考：本地 OpenCode 的 `tmp/opencode/packages/opencode/src/tool/registry.ts` 中 describeTask 筛选、排序并渲染 name + description，工具构建入口再追加到 Task 描述。Ash 借鉴“由创建工具生成可选名单”，不照搬 mode / permission 体系，不重复维护手写角色名单。

### C04：Skill 扩展与会话工具集合

- Profile 定义初始角色配置；Session 保存当前已安装能力。Skill 提供专项说明和额外工具，不是限制器，不能对它与 profile 的工具集合取交集。
- 首版 skill 的工具声明引用运行时已注册的工具实现；不从 Markdown 执行任意插件代码，不引入自定义工具语言。未知工具在安装前报错。
- 激活只扩展当前 Session；不修改共享 Agent 模板、其他实例或 profile。普通工具可按声明增加，创建下级等组织能力仍由调用身份检查。
- 工具实例、schema、配套说明及激活记录一起准备；全部成功才发布，下一个模型请求使用同一份完整快照。失败不留下部分安装。
- 同一来源的重复激活幂等；相同已注册工具复用，不重复安装；同名不同实现报错，不隐式覆盖。
- 新工具不在当前模型响应的同批调用中追溯生效；下一次模型请求看到它之后才可调用。
- 首版激活能力持续到会话结束；恢复重建已激活能力，恢复失败给出诊断，不静默缩减工具。暂不提供动态卸载工具。
- 不因本次 profile 重构改变无关的 Skill 模型字段行为；需要变更时单独列迁移，不暗中添加 profile model 配置。

### C05：组织身份与 Message 关系

实例 ID 寻址会话，profile 标识选择模板；名称不能冒充实例身份。Runtime 保存每个实例的直接 parent 和可选 group 归属，调用者不能通过正文或参数修改身份。

| 调用关系 | message 的行为 |
| --- | --- |
| parent → 直属独立 agent | 按 C06 发起、继续或打断 |
| parent → 直属 group 的成员 | 按 C06 在组级发起或改派，不只检查接收者是否空闲 |
| 当前组员 → 同组另一成员 | 按 C08 登记唯一后继 |
| 当前组员 → 自己预建的直属下级 | 正常委派；不占用同组后继槽位，下级若属其直属 group 则按组级规则处理 |
| 子 agent → parent | message 拒绝；以最后答复交回，parent 通过 wait 接收 |
| 自己、无关实例、非直属后代 | 拒绝；不绕过直接 parent，不把任意组外目标都误当成下级 |

- 主 agent 安装 agent/group 创建工具；Workflow 管理者通过已注册的管理能力只安装 workflow 创建入口，入口同时校验，不能直接 create/group。
- 普通子代理无论是否组员都不安装创建能力；无组无下级的叶子不安装协作工具。无下级组员只有 message/history/list，预建下级 / 组按关系追加管理工具，不赋予创建权。
- 创建能力不由 profile 的默认全部常规工具、Skill 或提示词继承；工具集合和注册目录一起收紧。蓝图事先确定层级，只有未启动实例入组时重新确定组织工具。
- 可用 profile 清单由实际创建工具负责：主 agent 是 agent，管理者是 workflow；协作说明按成员 / 协调职责安装，不给叶子重复灌完整手册。
- 一个 agent 最多加入一个组；组成员必须是该组 owner 的直属实例，同组不能含彼此的祖先 / 后代。
- 每个 Session 只由自己的控制入口驱动；禁止绕过 collab 向组员 Session 排入额外任务。
- list、history、wait 使用同一可见关系；成员可访问所属组及自己的预建直属下级 / 组，不暴露整个根组织或个人 transcript。

### C06：Message、改派与未读门禁

| 目标情况 | parent message | wait |
| --- | --- | --- |
| 从未启动 | 开始工作 | 立即返回暂无消息，不生成未读结果 |
| 正在运行 | 接受替代输入，取消旧 turn，收束后启动替代工作 | 按 C09 等目标这段连续工作停止 |
| 正在取消收束 / 接续提交 | 拒绝新的 parent 改派，提示稍后重发，不丢掉已接受输入 | 继续等待，不能将切换间隙当完成 |
| 已停止且最后结果未接收 | 拒绝并提示正确的 wait 目标 | 返回结果并接收 |
| 已停止且结果已接收 | 开始下一次工作 | 尚无新工作时重复返回相同结果 |

- 组内接续不经过 parent 的逐成员未读门禁；组结果只在整组停止时产生。
- parent 给组中任意成员发消息都受同一个组级门禁约束，不能换成员绕过 wait。
- 取消请求不等于已停止；旧 turn 和可取消工具收束后才能启动替代者，不创建新 Session、不回滚已有副作用。
- 完成、改派、后继登记和启动由同一控制入口裁决，按实际接受顺序处理；先完成则要求接收，先改派则继续替代工作。
- 改派清除旧后继；迟到事件只用于旧执行的内部结算，不能重新启动旧后继、覆盖新结果或改变新结果的接收状态。
- 工具校验失败直接返回错误；接受回执只表示已接受 / 已登记，不承诺对方已运行或业务成功。

### C07：Group 创建、文件和查询

- group 接口获取或幂等创建组，可逐次加入未启动的直属实例；空组合法，创建 / 加入不自动执行。
- 组 ID 在 owner 作用域内唯一，返回值须可稳定寻址；不同 owner 的同名组不能误绑定。运行数据使用受控存储键，不直接将输入当文件路径。
- 重复加入同一组幂等；已经启动、属于其他组、非直属的实例不得加入；运行中的组不变更成员。首版无迁移 / 移除成员接口。
- 每组有独立的 chat.jsonl 和 prompt.md，存于运行数据目录，不要求 Git；创建时返回实际路径，重复获取不清空文件。
- prompt.md 承载目标、背景和业务约束，可用普通文件工具维护；可有其他共享业务文件，但它们没有调度含义。
- collab 单点追加公共聊天：parent 输入、同组消息、成员最后答复及 runtime 诊断。保存消息标识、来源、发送者、可选接收者、正文及投递事实；不镜像推理或全部工具输出。
- 接收者用 agent_id 指定，不解析字面 @、不提供群发 / 私信。编辑聊天文件不会触发调度；被取消的投递不伪装为已执行。
- 接收者拿到当前消息及来源、组背景和关系摘要；需要前情时 history，不自动复制其他实例的个人历史。
- history 默认取最近 10 条并正序展示；before 为排除式消息锚点，未知 / 跨组锚点报错；limit 为正整数，上限 100 条及 64 KiB 输出。
- 查询返回继续翻页的锚点和截断标志，超长单条可截断显示但不改原记录；新消息追加不能使向前翻页重复前页。
- parent 显式传 group_id；组员省略时只查自身组，不猜测唯一子组。list / history 不接收最后结果，不解除未读门禁。

### C08：组内流转与下级协作

- parent message 某位成员开始；当前成员每 turn 最多向同组登记一个后继，登记不打断发送者，也不立即启动接收者。
- 重复 / 同批并发登记只有一次成功，其余报冲突；成员向自己的直属下级委派按 C05，不混入后继槽位。
- 当前 turn 正常结束才启动后继；失败 / 取消则清除后继并停止本段工作。后继启动失败也产生诊断，不回退到前一位成功消息。
- 无后继时组停止；有后继时收束与接续是控制器的一次状态转换，不向外发布瞬时空闲。
- 不固定 analyze / execute / verify 顺序，不理解 TODO 勾选率；运行规则之外由 agent 决定下一步。
- 当前成员不能 wait 自己、同组成员或自身所属 group，不能 wait 上级；可 wait 自己预建的直属下级 / 组。
- 求助时不登记后继，以最后答复说明问题；已登记的后继不会因后续自然语言自动撤销，发送者失败 / 取消或 parent 改派才使其失效。
- 组完成只涉及直接成员，不递归判断整棵组织树。启动下级的协调 agent 负责收齐结果；提前结束时运行中的后代仍须可见，不能宣称所有副作用已停止。

### C09：Wait 等待与交付

- wait 绑定指定目标的本段连续工作，而不是某个内部 turn。组内切换、取消后替代启动都不结束等待；目标真正停止后选定最后结果。
- 结果一旦在停止点选定，就固定交付该结果；之后真正发起的新工作不改变这次返回。多个等待者选定同一结果时返回一致的最后消息和结束状态；pending 为当前观察快照，可随后代变化。
- 执行标识只用于内部竞态处理；不暴露历史轮次 ID，不因旧 turn 被取消而提前把仍运行的目标报告为完成。
- parent 不能单独 wait 组员；工具报错并提示 wait(group_id)。独立 agent 与 workflow 主 agent 可用 agent_id 等待。
- 返回最后运行成员的最后对外消息；正常答复取最后一次 assistant 文本，不拼接整轮进度、不返回思考或工具输出、不调用模型总结。
- 失败、取消、截断、无正文结束或接收者启动失败均有明确结束说明；必要时由 runtime 产生并标注来源，不伪装成 agent 答复、不隐藏已有错误。
- 返回结构区分 message 与必要执行状态，但不判断业务是否成功；消息通常为文本，不要求 agent 填业务 schema。
- parent 的 wait 工具结果可靠记录后解除对应未读标记；这属于内部交付提交，不增加第二次用户确认或 ack 工具。故障不确定时允许重复返回，不丢结果。
- 同批 wait + message 不建立隐式执行依赖；结果尚未提交时 message 仍受未读门禁约束，应等 wait 返回后的下一次调用。
- 取消等待只取消等待，不取消目标；未交付的结果保持未读。等待不受普通短工具超时误杀，旧结果交付不能清除新结果的未读状态。
- wait 返回目标下方仍运行 / 未读的 pending，包含 owner_id、目标类型 / ID 和 state；只诊断、不接收后代，也不递归延长当前 wait。最多 32 项，超出标记 pending_truncated，并预留消息输出预算。
- list 顶层提供调用者下方的 pending。未启动且无结果的实例不视为未收尾；业务完成仍由 agent 判断。
- 根 turn 结束时，非交互 CLI 发现未收尾则诊断、正常收束后代并非零退出；交互界面只提醒，不自动发消息或无限催办。
- wait 的开始事件显示具体目标。尚无持久化工具结果可能只是 wait 未返回，不得直接判断未调用或死锁。

### C10：通用协作工具与最小返回内容

参数是具名字段，不实现位置参数重载。所有权限来自真实调用身份；空 ID、空 message、显式空 prompt 报错，省略 prompt 才表示只创建；wait 必须恰好选择一个目标。

| 工具 | 参数 | 最小返回内容 |
| --- | --- | --- |
| agent | profile?, prompt? | agent_id、实际 profile、parent_id、created / started；未传 prompt 时 created=true、started=false |
| message | agent_id, message | agent_id、可选 group_id、接受 / 登记行为及消息标识；不冒充完成结果 |
| group | group_id, agent_id? | 稳定 group_id、owner、成员、prompt_path、chat_path |
| history | group_id?, before?, limit? | group_id、消息列表、next_before、是否截断 |
| wait | agent_id?, group_id? | 目标、消息来源与标识、最后消息、执行状态、pending / pending_truncated；从未工作返回明确的暂无消息 |
| list | group_id? | 可见直属 agent / group，或指定组及成员；身份、profile、归属、运行与未读状态；顶层含 pending |

以上六个是 collab 通用工具，不是所有会话的工具总数上限。C11 另定义仅 workflow 管理会话使用的组织装配工具，它不是新的流程推进工具。

- 字段使用 snake_case；返回值的结构化外壳不强制消息正文结构。
- list 顺序稳定，组中成员不重复列成独立工作线；组详情显示唯一当前成员，空闲不标为业务成功。
- 参数 / 权限错误与执行后的失败结果分开；错误包含可操作的下一步和正确目标，不暴露内部编号或不可见组织详情。

### C11：Workflow 管理者、Skill 与装配工具

- 唯一基础启动方式是 agent(profile="workflow", prompt=任务)；/workflow 只是同一创建路径的便捷入口。外部 default 主 agent 保持原角色，用户无需先编写或选择 YAML 蓝图。
- workflow profile 的子 agent 是这次组织的创建决策者和管理者，对内为主 agent、对外为子 agent；复用同一套 Session，不增加外壳会话或特殊执行类型。
- 管理会话首次模型请求前就安装 workflow 装配工具和必要 collab 工具，并提供内置 workflow skill 的可发现信息；读取使用文档只追加内容，不中途新增 workflow 工具或重写基础 System。
- agents/workflow.md 定义创建者 / 管理者职责；skills/workflow/SKILL.md 说明何时装配、蓝图如何编写、工具用法和示例；workflow 工具说明保留简短用途与参数 schema。三者各自维护，不重复整份说明。
- 主 agent 接收任务后读取 skill，分析需求并拆解工作，动态制定组织蓝图；runtime 不代替 AI 决定任务分工，只校验和执行装配。
- workflow 工具接受主 agent 制定的 blueprint，返回管理者 ID、装配结果、蓝图局部名称到真实 agent / group ID 的对应及共享文件路径；不另外创建管理者、不自动启动成员、不等待业务完成。
- 组织归实际调用的 workflow 管理者所有，下级保留蓝图指定的直接 parent；不能把所有新实例挂到外部 default 主 agent 下。
- 蓝图描述主 agent 之下的子 agent、各层下级、group、成员关系和共享背景；每个新节点创建时解析 profile，缺省 default。
- “预先创建完整组织”指下级正式执行任务前，而不是管理者开始思考前。顺序为：agent 创建管理者 → 读取 skill 并生成蓝图 → 调用 workflow 工具 → 校验并装配全部下级与组 → message 委派。
- 开始下级执行前校验全部定义、局部名称、引用、parent 树无环及 C05 的成员归属；装配期间主 agent 已存在，下级保持未运行，失败只清理本次未运行资源并将错误交回主 agent。
- 装配成功后跨组的顺序、并行和取舍由管理者通过 message / wait 决定；同组由当前成员登记后继。各级协调者复用 collab，不新增 run_workflow、next_step 或另一套调度 / 聊天存储。
- 外部 default 通过 message(workflow_agent_id, ...) 和 wait(agent_id=workflow_agent_id) 与管理者交互；workflow 工具不能兼任第二套子会话启动入口。
- 普通成员不能动态创建下级；主 agent 的重复 message 不自动重建组织。后续规划不能静默替换仍运行的成员或重投已有工作。
- 主 agent 提前结束 / 被打断不等于后代停止；后续 turn 可 list / wait 接管。C08 定义组的完成边界。
- 蓝图是 AI 产生的组织数据，不是新的业务流程语言；如使用 YAML 表达，只描述组织，不包含循环、条件或可执行脚本，不把外部 YAML 加载器当 /workflow 的必需前置。
- 移除 JS 专有生命周期、旧工具与依赖；不保留旧入口、只读兼容层或迁移器。只有获得 workflow 管理能力的会话安装装配工具，普通会话和成员不自动继承；读取同名 skill 不能取得这项组织权限。

### C12：存储、恢复与观察

- Session 负责个人历史和执行；collab 保存组织关系、聊天、执行关联、最后结果及接收事实；workflow 通过 collab 的同一组织提交保存已生成蓝图与装配关联，不另建存储或重复持有成员执行状态。
- 创建时保存固定 profile，按 C04 保存激活能力；恢复使用实例记录，不从最新同名角色重建旧行为。
- 区分消息已接受、已开始、已停止、结果已交付；聊天存在不代表工具已经执行。实时控制状态、持久化事实和聊天各司其职。
- 恢复完整结束结果及未读状态；不完整执行转为可接收的 interrupted / unknown 诊断，不永远等待已不存在的执行者。
- 不根据聊天重放 message，不重跑 shell，不自动启动未完成后继，不承诺外部副作用恰好一次。
- 记录失败要如实报错；消息未可靠接受时不启动接收者，运行后存储失败不能宣称可靠完成或继续自动后继。
- UI、list、wait 使用同源状态 / 事件；重连由快照补齐，界面不独立维护业务成功和接收状态。history 展示公共交流事实。
- 停止保留会话、聊天和工作目录；关闭是 runtime / UI 生命周期动作，不伪装成 message 或 wait，不自动回滚文件。

## 二、实施任务

下表划分职责而非新增 crate；只把稳定跨模块共享类型放入 ash-core。每项先做针对性测试，任务完成必须满足对应 C 契约；不修改无关代码和既有用户改动。

| 模块 | 实施职责 | 不承担 |
| --- | --- | --- |
| ash-core | 必要 ID、事件和工具边界类型 | 全部协作状态或万能插件容器 |
| ash-agent | Profile、提示词、Session 能力快照、执行和个人持久化 | 组织调度、UI 状态 |
| ash-tools | 已注册工具实例、文件路径相关规则发现 | 协作规划和组织权限推断 |
| ash-collab | 六工具、关系校验、组控制、接收及公共聊天 | 另一套模型循环 |
| ash-workflow | workflow profile、内置 skill、组织装配工具和安装入口 | 特殊 Session 类型、另一套消息 / wait 实现、普通会话的全局配置 |
| ash-cli / ash-tui | 配置选择、安装入口和同源事件展示 | 各模块提示词正文 |

### P0：提示词基础（最高优先级）

- [x] P0-01：为 `ash-agent/src/prompt.rs` 和 `ash-cli/src/modes.rs` 的当前内容建立回归基线，标明 C03 要保留的规则及要迁移的来源。
- [x] P0-02：迁移 CLI 的协作正文，复用 workflow 自有正文；新增工具 / 能力的显式安装入口，组合器不再维护业务分支。（C03）
- [x] P0-03：以工具名 / 规则文件路径作稳定片段键，实现冲突校验；新装配失败不发布半成品，不为去重建立通用插件框架。（C03、C04）
- [x] P0-04：在模型请求边界统一使用工具、说明和动态上下文快照，覆盖重试 / 压缩；保留可解释的 turn 上下文记录。（C03、C04）
- [x] P0-05：扩展 AGENTS.md 发现与作用域测试，接入文件工具路径检查和未见规则时的写入延后；保留真实 cwd、编码 / 截断 / 读取错误处理。（C03）
- [x] P0-06：agent 工具生成可选角色摘要，复用单一筛选 / 排序函数；普通工具参数描述与行为说明不重复膨胀。（C03、C10；依赖 P1-01）

### P1：内置 Profile 与 Skill 扩展

- [x] P1-01：新增 `ash-agent/src/profile.rs` 及三个内置 Markdown 文件；复用 frontmatter 解析基础，不合并 Profile / Skill 类型或改动无关 Skill 发现规则。（C02）
- [x] P1-02：实现创建时默认解析、校验与角色固定，接入根会话 / 普通子 agent；冻结 profile 工具配置和 schema 回归测试。（C02、C10）
  - [x] 根会话 `--profile`、agent 工具可选 profile、未知 / 空值校验、独立角色定义、后续消息沿用角色及捕获模型请求的工具集回归。
  - [x] 新创建协议支持省略 prompt；持久化解析后的角色与能力，恢复不改用最新版同名角色。完整 schema 序列化审查见 P6-03。
- [x] P1-03：在 `ash-agent/src/skill.rs` 与装配入口移除 skill 工具限制路径，将名字解析到既有工具实现；不引入新工具语言。（C04）
- [x] P1-04：为 Session 实现独立的能力安装提交，接入 `engine.rs` 的 schema 生成与工具执行查询；同批调用使用原快照，下一请求才看到新增工具。（C04；依赖 P0-04）
- [x] P1-05：实现激活失败回退、幂等和同名冲突检查；建立可恢复激活记录，证明同模板的其他 Session 不受影响。（C04、C12）
- [x] P1-06：审阅三份角色正文，并使用 Grok 完成同一小型真实任务的角色差异验收；通过内置打包、工具集和不可热切换测试，不把字符串快照当角色质量证明。（C02）
  - [x] 内置 Markdown 编译、工具集合及子代理连续消息的配置固定测试；explore / review 保留 bash，不宣称只读沙箱。
  - [x] 恢复旧实例保持已保存角色、工具和说明，缺失工具 / 已运行子记录明确报错。
  - [x] 2026-09-10 使用同一发票缺陷验收：default 修复且 4 项测试通过，explore / review 只定位并复现问题，不改源码；小样本不代表通用质量保证。见 `GROK_EVALUATION.md`。

当前落地：工具和说明共同安装；具路径 / 作用域的 AGENTS.md 在文件操作前发现，同来源更新替换旧版；Session 独立提交能力快照。六工具、group 串行执行线、workflow 管理者 / skill / 装配器均已接入，旧 JS 执行器和依赖已删除。group 与 chat 的路径使用 runtime ID，不提供 worktree 隔离。

接收时序：wait 选定停止结果 → parent TurnStep 和能力快照落盘 → committed 按结果消息 ID 清除未读并落盘 → 下一请求。恢复无法从 parent 的完整 turn 证明交付时保留未读，允许重复接收，不重放副作用。

自动化验证命令：`cargo test --workspace`、`cargo clippy --workspace --all-targets -- -D warnings`、`cargo fmt --all -- --check`、`git diff --check`。模块测试覆盖清单见第五部分；未完成的人工和故障组合验收不自动勾选。

### P2：普通委派与结果交付

- [x] P2-01：在 `ash-collab/src/control.rs` 迁移 agent / message / wait / list 的工具名、参数和返回类型；保存直接 parent 并统一校验，不把旧 root 容器当新 group。（C05、C10）
- [x] P2-02：实现同一控制入口的运行 / 停止 / 未读判定、改派接受与取消窗口；复用 Session turn 取消，不改写模型循环。（C06）
- [x] P2-03：实现目标连续工作等待、停止点结果固定、多等待者和无丢唤醒；内部执行编号只过滤迟到事件。（C09）
- [x] P2-04：实现最后答复提取与 runtime 诊断，不能直接复用汇总整轮文本的 visible_text；冻结正常 / 失败 / 取消 / 无正文返回。（C09、C10）
- [x] P2-05：画出 wait 返回、parent 工具结果落盘、接收状态提交的内部时序并实现关联；崩溃不确定时保留可重读结果，不增加模型工具。（C09、C12）
- [x] P2-06：核对取消工具和进程收束；为取消等待、改派失败和存储失败补测试，不将 drop future 当作外部副作用已撤销。（C06、C09）

### P3：Group 文件与串行控制

- [x] P3-01：实现 group 的 owner、稳定 ID、幂等建组 / 入组和共享路径返回；为同名不同 owner、空组、错误归属及路径输入建立测试。（C05、C07、C10）
- [x] P3-02：实现单点聊天追加、消息 / 执行关联和 history 分页；读取 / 编辑聊天不能投递工作；prompt 读取失败转为诊断。（C07、C12）
- [x] P3-03：在 collab 内实现最小执行线控制（AgentControl + Line），持有当前执行、唯一后继 / 已接受替代输入和最后结果；不建立业务状态机。（C06、C08）
- [x] P3-04：通过同一控制入口处理登记、结束、改派和启动失败；原子接续、旧后继失效、迟到事件过滤均以可控时序测试。（C06、C08）
- [x] P3-05：接入组级等待 / 未读门禁与成员等待拒绝；list / history 不改变结果接收，回执和最终结果不同。（C07、C09、C10）
- [x] P3-06：按 C05 关系表测试同组接续与直属下级委派，不再笼统拒绝所有组外目标；覆盖等待自己 / 所属组 / 上级的自锁拒绝。（C05、C08）

### P4：Workflow Profile、Skill 与组织装配

- [x] P4-01：注册 workflow profile，创建时安装所需能力；将 /workflow 接到 agent(profile="workflow") 同一路径，普通主会话只看到可选角色摘要，不安装装配工具。（C02、C03、C11）
- [x] P4-02：实现 workflow(blueprint) 装配工具、参数 / 返回契约及错误示例；校验默认角色、重复引用、循环和归属，失败时让主 agent 获得诊断，不启动下级任务。（C02、C05、C11）
- [x] P4-03：主 agent 规划之后递归预建多层下级组织及共享文件；全部成功才允许成员执行，返回真实实例 ID，失败不销毁主 agent 或已有组织。（C11）
- [x] P4-04：让 ash-workflow 单向复用 collab，装配层提供最小创建接点，不形成循环 crate 依赖；运行中无第二套调度器。（C11）
- [x] P4-05：各级管理者按关系安装 collab 工具，workflow 专用提示词仅给专用主 agent；测试预建下级、禁止成员动态创建及后续 message 不重复装配。（C03、C05、C11）
- [x] P4-06：管理者提前结束 / 打断时后代接管的自动化集成回归通过；终端人工验收归 P5-05。（C08、C11）
  - [x] 移除旧 JS 工具、VM、专有存储及依赖；没有兼容别名或只读迁移器。
  - [x] 后代独立执行、直属下级可见和禁止跨级访问已实现。
  - [x] `stopped_or_replaced_coordinator_can_collect_its_running_descendant` 覆盖后代阻塞时管理者结束 / 改派、新 turn 查询及 wait 接管，并核对同源显示快照。
- [x] P4-07：在 ash-workflow 内提供 agents/workflow.md 与 skills/workflow/SKILL.md 并随二进制打包；工具在首次请求前装好，skill 只负责加载使用说明，测试加载前后工具定义及基础 System 不变。（C02、C03、C04、C11）

### P5：恢复与观察

存储提交边界随 P1–P3 实现，不等本阶段才设计；本阶段验证完整恢复和展示。

- [x] P5-01：在现有 JSONL / 协作存储中保存 profile、能力、关系和接收事实；复用已有记录与 ID，不仅从聊天反推执行。（C02、C04、C12）
- [x] P5-02：重建整个可见组织及各子 Session，缺失定义 / 工具 / 子记录给诊断；不只复用根 Session resume 就宣称完成恢复。（C12）
- [ ] P5-03：补齐消息与接收提交窗口的组合故障注入。（C06、C09、C12）
  - [x] 接受 / 执行中快照恢复为 interrupted，不重发模型请求；聊天残尾修复。
  - [x] 完成未读、wait 同批门禁、旧接收不清新结果、父记录交付证明、缺失子记录和改派存储失败回退。
  - [ ] 用真实 shell 文件副作用与进程强杀覆盖“副作用已发生但完成未落盘”，核对不重跑。
  - [ ] 对状态 rename / 目录 fsync、个人 Step / TurnEnd 与接收保存之间逐点注入故障，核对重复交付和诊断。
- [x] P5-04：扩展 collab 事件和 UI 快照，展示运行、停止、异常、后代和未读；重连结果与 list / wait 一致。（C08、C12）
- [ ] P5-05：安装包与交互生命周期验收。（C01、C02、C12）
  - [x] 协作测试使用非 Git 临时目录；角色 / workflow skill 通过 include_str 打包；CLI 关闭收束旧根工作，不引入 Git 操作。
  - [x] 脱离源码目录、在独立 HOME / 非 Git 项目运行编译产物；真实 Grok 验证角色、skill、group、/workflow，无运行时源码读取或外部 YAML 前置。
  - [ ] 手工核对交互 TUI 的 /workflow、Esc 改派、退出与恢复时的终端展示；非交互 run 不能代替这项验收。

### P6：收尾与集成

- [x] P6-01：同步 `COLLABORATION.md`、`DESIGN.md`、工具说明和示例；删除旧轮次等待、旧工具名、skill 限制器和组员只能同组通信等旧约定。
- [x] P6-02：模块针对性测试及全量测试通过（当前 498 个）；`cargo fmt --all -- --check`、`cargo clippy --workspace --all-targets -- -D warnings`、CLI `--help` 和 `git diff --check` 通过；不顺手修无关问题。
- [ ] P6-03：逐项核销第三部分 T01–T37，补齐完整六工具 schema / 返回值快照及未覆盖的故障组合；已有核心 / 协议序列化回归不等于新增 API 快照全覆盖。

实施顺序：P0 基础与 P1 Profile → P0-06 / P1 Skill 扩展 → P2 普通委派 → P3 Group → P4 Workflow → P5 完整恢复 / UI → P6。Workflow 通过 agent(profile="workflow") 启动，/workflow 是快捷方式，不再等待外部蓝图选择方案。

## 三、验收场景

表中只列操作与断言，行为定义回到 C 契约；并发使用 mock 模型、barrier / 通知，不靠真实模型或 sleep 猜时序。

| 测试 | 操作与关键断言 | 契约 |
| --- | --- | --- |
| T01 默认角色 | agent 省略 profile、显式 default、空名、未知名；前两者一致，错误不创建实例 | C02、C10 |
| T02 工具集合 | 三角色常规工具与默认集合比较，explore / review 仅少 write、edit | C02 |
| T03 角色文件 | 缺 description、空正文、未知字段 / 工具、空 tools、重复工具、无源码目录启动 | C02 |
| T04 角色固定 | 替换测试注册表或升级二进制后恢复旧实例；旧实例保持原定义，新实例使用新定义，不测试不存在的源文件热加载 | C02、C12 |
| T05 提示词来源 | 安装 / 不安装能力、重复来源、agent 名称摘要；工具与说明一致，其他角色正文不泄漏 | C03 |
| T06 仓库规则 | 根 / cwd / 更深目录 / 兄弟目录 / 超长 / 不可读规则；新规则先呈现再写入，同批写入也不能绕过 | C03 |
| T07 动态上下文 | 多次请求、重试、压缩、下一 turn 修改 group prompt；不累积、不丢当前作用域 | C03 |
| T08 Skill 扩展 | 激活后下一请求可用，既有工具保留，重复幂等，同批未曝光工具不可提前调用 | C04 |
| T09 安装失败与隔离 | 未知工具、同名不同实现、部分准备失败；无半安装，同模板其他 Session 不受影响 | C04 |
| T10 空目标 | 从未运行的 agent / group wait 立即返回暂无消息，history 空列表，无假未读 | C07、C09 |
| T11 未读门禁 | 完成 → message 拒绝 → list / history 后仍拒绝 → wait → message 成功 | C06、C09 |
| T12 连续等待 | A 执行 → wait 挂起 → 改派 B → B 停止；wait 不返回 A 的取消，最终交付 B | C06、C09 |
| T13 取消窗口 | 已接受改派尚未收束时再发消息；明确拒绝，已接受输入不丢、不出现双运行 | C06 |
| T14 完成竞争 | 完成与 message 竞争；按控制顺序得到改派或先接收错误，不丢唤醒 | C06、C09 |
| T15 重复等待 | 多 wait、重复读取、结果选定后启动新工作、取消等待；旧结果交付不清除新结果未读 | C09 |
| T16 最后消息 | 进度 → 工具 → 最后答复，另测失败 / 取消 / 截断 / 空正文；不返回整轮拼接或隐藏异常 | C09 |
| T17 建组 | 空组后加成员、重复调用、不同 owner 同名、错误归属、运行时变更、路径输入；共享文件不被清空 | C05、C07 |
| T18 自然流转 | A → B → A 和无后继结束；组级 wait 不返回中间成员，最后结果只产生一次 | C08、C09 |
| T19 后继竞争 | 同批两次后继、parent 改派、旧 turn 迟到结束；唯一合法下一执行者 | C06、C08 |
| T20 后继失败 | 登记者失败 / 取消、接收者启动失败、prompt 读取失败；停止并交付诊断，不默默回退成功 | C07、C08、C09 |
| T21 组接收 | 完成未读时换成员 message 仍拒绝；parent 单独 wait 组员报错并提示正确目标 | C06、C09 |
| T22 关系通信 | 组员 message 同组成员、直属预建下级、上级、无关实例；各自符合关系表 | C05 |
| T23 自锁与求助 | wait 自己 / 所属组 / 上级拒绝；最后答复求助使 parent 正常收到，不反向重入 | C05、C08、C09 |
| T24 历史分页 | 最近 10 条、追加后翻页、跨组锚点、超长单条；无前页重复、原文不被截断写回 | C07 |
| T25 返回契约 | 六工具正常 / 空结果 / 错误的稳定结构，group 路径可被 parent 用于维护 prompt | C10 |
| T26 动态蓝图 | /workflow 接收任务，mock 主 agent 按不同任务产生不同组织；主 agent 先运行规划，所有下级执行前完整装配，无需外部 YAML | C05、C11 |
| T27 装配失败 | 引用错误、重复归属、parent 环、资源创建失败；下级不启动，主 agent 收到诊断，不删除已有资源 | C11 |
| T28 层级接管 | 组员拥有预建下级但不能动态创建；协调者提前结束或被打断，后代仍可见并可继续接管 | C05、C08、C11 |
| T29 消息恢复 | 接受后未启动、交接中、shell 已修改但完成未落盘；返回中断诊断，不重投历史输入 | C12 |
| T30 接收恢复 | 完成未读、wait 返回持久化前后故障；结果不丢，必要时重复交付，不错误解除新工作门禁 | C09、C12 |
| T31 能力恢复 | 已激活 skill、旧 profile、缺失工具实现 / 子记录；重建成功或明确诊断，不静默换角色 / 缩减工具 | C02、C04、C12 |
| T32 多组与显示 | 两组并行，一组异常 / 取消 / 记录失败；另一组可推进，UI 重连与查询一致，不把空闲标成成功 | C01、C12 |
| T33 首版边界 | 非 Git 目录协作、旧 JS 入口、模型工具列表；无隐式 Git 命令、无新增推进 / ack 工具 | C01、C11 |
| T34 模块隔离 | 普通会话、workflow profile 管理者、内部成员分别检查工具与 System；仅管理者装有 workflow 工具和角色正文，外部角色摘要包含可选 workflow | C02、C03、C11 |
| T35 入口一致 | agent(profile="workflow") 与 /workflow 均创建同配置管理会话；workflow 工具只装配其下级，不另起管理者 | C02、C10、C11 |
| T36 Skill 文档加载 | 管理者首次请求已含 workflow 工具；读取 skill 后基础 System / 工具定义不变，后续上下文包含用法 | C03、C04、C11 |
| T37 组织归属 | 管理者装配多层组织，成员未自动运行且 ID / 路径完整返回；外部 default 不成为所有成员的直接 parent | C05、C07、C11 |

## 四、待定项

当前没有需要用户选择外部蓝图的入口待定项。agent(profile="workflow") 创建管理者，/workflow 为快捷入口；管理者读取 skill、根据任务制定蓝图、调用 workflow 工具装配，具体行为以 C11 为准。

蓝图的数据字段和提交接口在 P4-02 中以最小可测试方案落实；它们是 workflow 模块的实现任务，不再把“由谁制定蓝图”或“主 agent 何时启动”留给执行者猜测。其余实现细节按对应契约处理，不重新引入外部静态配置前置或通用流程语言。


## 五、实现定位与补充验收

| 范围 | 实现 / 自动化入口 | 当前覆盖 |
| --- | --- | --- |
| 角色与提示词 | ash-agent/src/profile.rs、prompt.rs、agent.rs；ash-tools/src/instructions.rs | 内置解析、默认路径、工具选择、模块顺序、作用域规则、同源更新去重、读取失败和写入延后 |
| 会话能力 | ash-agent/src/engine.rs、session.rs、jsonl.rs | 原子能力安装、同批不曝光、Session 隔离、已提交快照恢复与旧角色保持 |
| 协作 API | ash-collab/src/api.rs、control.rs、tests.rs | 六工具、身份、未启动实例、未读门禁、连续 wait、取消等待、旧接收隔离 |
| 组执行线 | ash-collab/src/state.rs、control.rs、tests.rs | 串行后继、parent 改派、同批竞争、取消窗口、prompt 故障、多组并行与同源快照 |
| 共享存储 | ash-collab/src/store.rs、tests.rs | UUID 路径、owner 隔离、幂等、history 排他分页与 64 KiB 输出、完整原文保留 |
| Workflow | ash-workflow/src/lib.rs、tests.rs；ash-cli/src/modes.rs | 首请求装配工具、skill 不改工具/System、完整预建树、归属校验、非法蓝图无部分发布、快捷入口复用 |
| 恢复 | ash-agent/src/jsonl.rs、ash-collab/src/control.rs、tests.rs | 中断不重放、残尾修复、缺失已运行子记录报错、接收证明与能力定义恢复 |
| 界面 | ash-cli/src/modes.rs、ash-tui/src/app.rs | 一组一行、过期事件过滤、落后订阅快照补齐、根会话关闭；真实终端仍待手工验收 |

下一批只补验收，不再重新设计 API：

1. 小型真实任务的三角色差异已验证；继续在更复杂真实仓库任务上扩大样本，不把本轮视为稳定成功率证明。
2. 独立临时目录、非 Git 与动态蓝图已验证；继续保留完整安装包 / 交互终端的手工验收。
3. 自动化已覆盖管理者结束 / 改派后的后代接管；手工核对真实终端的后代显示与展开信息。
4. 以 shell 创建标记文件后强杀进程；resume 后只返回 interrupted，标记不因自动重跑变化。
5. 分别在状态提交和 wait 接收提交的边界注入 IO 错误；结果只能可靠交付或保留可重读诊断。
6. 对新增工具的完整 JSON schema 与返回结构补稳定快照，并逐项给 T01–T37 添加用例名称，不把一个总测试命令当全部场景的证明。

## 六、Grok 实测反馈

2026-09-10 按 `ash -m grok --protocol openai-responses --max-context-tokens=500000` 完成 11 类场景、17 次 CLI 运行；完整结果和失败样本索引见 `GROK_EVALUATION.md`。

- [x] 可选 SessionId schema 改为内联 string/null，保留强类型；message / group / wait 增加回归。
- [x] agent 创建回执改为 created / started，避免“创建成功但 accepted=false”的歧义；增加未启动创建回归。
- [x] 明确组员不能等待所属组，但管理者可以等待其拥有的组；强调 list/history 不接收结果。
- [x] Workflow 外部协调正文归 ash-workflow 的 manager_input，CLI 不维护这段业务提示词。
- [x] 最后双组复测：管理者实际 wait 两组，两组已读，8 项测试通过；外部 parent 仅 wait 管理者。
- [x] Responses debug 只记录完成输出的类型 / 工具名，便于区分未发调用与解析遗漏，不写正文、参数或认证信息。
- [ ] 继续扩充复杂任务的重复样本：本轮存在提前结束、反复催办和超时的失败样本，不能用最后一次通过宣称稳定。
- [x] 追加跨模块订单服务验收：两个并行实施 / 复核组 + 独立集成者，预置 30 项测试；详细、精简指令两次 Workflow 都未完成闭环，失败样本保留，见 `GROK_EVALUATION.md` 追加章节。
- [x] 同一业务做单 default 对照：仅取消组织方式要求，30 项测试及 100 组外部固定种子组合通过，测试 / 规范未修改；不能视为 Workflow 成功。
- [x] 优先收尾诊断：已用 pending 暴露停止目标下方仍运行 / 未读的工作及 owner，不增加业务状态机或自动推进工具；不保证模型一定遵循收尾提示。
- [x] 优先根会话收尾验收：新增未收取工作时的非零退出与诊断；真实 Grok 再现提前结束后返回 1，而非旧行为的静默 0。无委派或已收齐时仍正常退出。
- [ ] 在上述问题明确后重测跨阶段集成、review 会话复用和真实发现缺陷后的返工；本次 Workflow 未进入这些阶段，不计为已验证。
- [x] 重复最小 Workflow 收尾测试两次：同一任务一次完整接收，一次 parent 无工具调用即退出并取消 manager；不能仅用“任务复杂”解释提前结束。
- [x] 完整订单单组对照：实施 → history 复核及 30 项测试通过，外部 100 组随机验收通过；480 秒上限时 reviewer turn 未结束、parent wait 未返回，整体闭环不计通过。
- [x] 本轮重新构建并运行 `cargo test -p ash-collab wait`（6 项）和 `cargo test -p ash-workflow`（3 项），全部通过；模型工具选择可靠性另行验收。
- [x] 收尾诊断区分“未发起 wait”和“wait 已发起但尚未返回”：开始事件显示等待目标，pending 只报告运行 / 未读事实；限时未完成不自动归类为死锁。
- [ ] 追踪最小组织偏差：请求 default + review 时首批两个空参数 agent 调用创建了两个 default，补建 review 后遗留未启动实例；保留当前可选 profile 契约，不因单样本直接改 API。

## 七、组织能力收紧与收尾诊断

- [x] 移除按 member 布尔值决定全部能力的接口；主实例显式 install_root，其他实例按管理能力、成员关系、预建下级 / 组推导 Access，不增加 profile 权限字段。
- [x] 默认 / explore / review 普通叶子不安装协作工具；组员无下级时只有 message/history/list；有预建下级才能 wait 下级，不能动态创建。
- [x] Workflow 管理者只通过 workflow 创建；agent/group 入口仅根身份可用。profile 清单跟随真实创建工具，成员说明与协调说明分开注入。
- [x] 不允许的组织工具从注册目录一并移除；能力扩展和恢复不能重新安装。角色本身与常规能力仍保留，不把 Skill 当 profile 限制器。
- [x] pending 区分 agent/group、owner、running/unread，组员不重复统计；wait 的最后答复保持不变，后代仍运行也不延长组 wait，查看不接收。
- [x] 非交互根收尾诊断与取消清理有 running/unread/received/无委派用例；交互提醒不触发自动派发或取消。保持已发起 wait 的取消语义。
- [x] 全量 498 项测试、格式、Clippy 通过；新增真实模型复测：47.77 秒最小 Workflow 正确闭环，22.92 秒独立委派提前退出被诊断为非零，后者不标为任务成功。
- [ ] 手工核对 TUI 警告与输入框状态、实际终端下的长时间 wait 展示；本次只做自动化与非交互复测。
- [ ] 在新工具 / 提示词范围下重测复杂订单、多组集成及 reviewer 会话复用；不能用最小成功样本宣称稳定完成率。
- [x] 再次真实复测权限和两层 wait：leaf 能力保持收紧；Workflow 的 group wait 已返回且组已读，但 manager 尚未结束，parent 未继续 wait manager，根收尾以退出码1正确报告，见 `GROK_EVALUATION.md` 追加章节。
- [ ] 补 Workflow manager → parent 的最小重复样本：区分“group 已完成、manager 仍在处理 wait 结果”和“manager 已最终结束”；不自动转发 group 结果或续跑 manager。
